<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="getlangns">
	<resultMap id="getlangResult" type="getlang">
		<result property="lang_no" column="lang_no" />
		<result property="lang_name" column="lang_name" />
	</resultMap>

	<select id="lang" parameterType="string" resultType="getlangResult">
		select
		lang_no, lang_name from member m, language l where m.id = #{id} and
		m.lang_no = l.lang_no
		union select lang_no, lang_name from inter_lang
		i, language l where i.id = #{id} and i.lang_no = l.lang_no
	</select>
</mapper> 